$("a[href='<%= new_post_comment_path(@post) %>']").after("<%= escape_javascript(render 'comments/commentform') %>"); //récup lien page html et ajout (after) d'un formulaire form
$("form[data-remote].commentform").attr("data-remote", true);	//récup du form en rajoutant attribut data-remote ajax (si true, soumettre avec AJAX)
$("a[href='<%= new_post_comment_path(@post) %>']").fadeOut()	//cacher le lien puisque le formulaire création y est déjà
