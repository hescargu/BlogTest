$("#notepost").hide();
$("#notepost").after("/5").after(<%= escape_javascript(sprintf("%.2f",@post.note))%>);
$("form[data-remote]").remove();
$("a[href='<%= new_post_mark_path(@post.id) %>']").fadeIn();
